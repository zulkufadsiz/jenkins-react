pipeline {
    agent any
     environment {
      PATH='/usr/local/bin:$PATH'
    }
    stages{
        stage('Build'){
            steps {
                 sh 'npm install'
            }
        
        }
    }
}
