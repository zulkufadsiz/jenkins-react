pipeline {
    agent any
    stages{
        stage('Build'){
  
            steps {
                 sh 'pwd'
                sh 'env'
                 sh '/usr/local/bin/npm install'
            }
        }
    }
}
